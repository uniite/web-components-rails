source 'https://rubygems.org'

# Dependencies are specified in the gemspec file
gemspec

# Allows us to test against multiple rails and sprockets versions
if rails_version = ENV['RAILS_VERSION']
  gem 'rails', "~> #{rails_version}"
end
